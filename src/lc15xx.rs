pub fn num_ways(s: String) -> i32 {
    // let mo: usize = 1000000007;
    // let mut vec = vec![];
    // for i in 0..s.as_bytes().len() {
    //     match s.as_bytes()[i] {
    //         49 => { vec.push(i); }
    //         _ => {}
    //     }
    // }
    // let num = vec.len();
    // if num % 3 != 0 {
    //     return 0;
    // }
    //
    // return if num == 0 {
    //     (((((num - 1) % mo) * ((num - 1) % mo)) / 2) % m) as i32
    // } else {
    //     let idx = num / 3;
    //     (((vec[idx] - vec[idx - 1]) % mo)
    //         * ((vec[idx * 2] - vec[idx * 2 - 1]) % mo)) as i32
    // };
    1
}
